// limitation of array if we not know n then we can't proceed still if we proceed then might be shortage or excess of memory
#include<stdio.h>
int main()
{
	int n,a[n];
	printf("a");
	return 0;
	
}
