// starting in mind
#include<stdio.h>
int main()
{
		int n;
		float i;
		int b;
		int d;
		int l[]
		printf("enter a number");
		scanf("%d",n);
		for(i=1;;i++)
		{
			b=pow(10.0,i);
			if((n/b)==0)
			  break;
		}
		// No. of digits in number is 'i'
		d=i;  // making integer type
		for(;d>=1;d--)
		{
			(n/pow(10.0,d+1))
		}
}
